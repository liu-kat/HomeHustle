$(document).ready(function () {
  $(".hero-slider").slick({
    autoplay: true,
    autoplaySpeed: 2500, // Adjust speed as needed
    arrows: false, // Remove navigation arrows if not needed
    dots: true, // Add navigation dots
  });
});
